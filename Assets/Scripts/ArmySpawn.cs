using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ArmySpawn : MonoBehaviour
{
    //public Transform infantryLeftSpawn, infantryRightSpawn;

    //[SerializeField]
    //GameObject infantryPrefab,dragonPrefab,leftLineParent, rightLineParent;
 
    //void Update()
    //{
    //    if(FindObjectOfType<ArmyCount>().currentInfantryCount % 2 == 0 && !(FindObjectOfType<ArmyCount>().currentInfantryCount==0))
    //    {
    //        GameObject clone = Instantiate(infantryPrefab, infantryLeftSpawn.position,Quaternion.identity);
    //        clone.transform.parent = leftLineParent.transform;
    //    }
    //    if (FindObjectOfType<ArmyCount>().currentInfantryCount % 2 == 1)
    //    {
    //        GameObject clone = Instantiate(infantryPrefab, infantryRightSpawn.position, Quaternion.identity);
    //        clone.transform.parent = rightLineParent.transform;
    //    }
    //}
}
